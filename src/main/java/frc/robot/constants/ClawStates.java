package frc.robot.constants;

import frc.robot.util.ClawState;

public class ClawStates {
    
    // Intaking states
    public static final ClawState CORAL_INTAKING_STATE = new ClawState(
            0, 
            0);

    public static final ClawState ALGAE_INTAKING_STATE = new ClawState(
            0, 
            0);

    // Stowed states
    public static final ClawState CORAL_STOWED_STATE = new ClawState(
            0, 
            0);

    public static final ClawState ALGAE_STOWED_STATE = new ClawState(
            0, 
            0);

    // Scoring states
    public static final ClawState CORAL_SCORING_STATE = new ClawState(
            0, 
            0);

    public static final ClawState ALGAE_PROCESSOR_SCORING_STATE = new ClawState(
            0, 
            0);

    public static final ClawState ALGAE_NET_SCORING_STATE = new ClawState(
            0, 
            0);

}
